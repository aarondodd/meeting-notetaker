PyQt6>=6.5.0
PyAudio>=0.2.14
PyAudioWPatch>=0.2.12.7; sys_platform == 'win32'
faster-whisper>=1.1.0
numpy>=1.24.0
webrtcvad-wheels>=2.0.14
pyperclip>=1.8.2
sounddevice>=0.4.6
tomli>=2.0.0; python_version < '3.11'
# Make Python's ssl module use the OS certificate store so corporate MITM
# proxies (Netskope, Zscaler, etc.) work without manual CA bundles. Pure
# Python wheel; no compiled artifact.
truststore>=0.9.0; sys_platform == 'win32'
# Outlook calendar integration via COM. Windows-only; provides win32com.client.
pywin32>=306; sys_platform == 'win32'
# Speaker embedding model for post-meeting speaker identification.
# Pulls librosa, scipy, torch transitively. Used only during the
# refinement pass on the loopback channel; the embedding model is small
# (~17MB of weights) but the dep footprint is non-trivial -- see the
# Wheel Manifest section in README.md for the IT-review summary.
Resemblyzer>=0.1.4
