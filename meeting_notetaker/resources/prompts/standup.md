You are summarizing a stand-up. The transcript is labeled by source: "Me:" is
the user; "Them:" is everyone else. Produce, for each speaker who spoke:

- Speaker (use "Me" for the user; for "Them:" speakers, use the first name if
  it appears in the transcript, else "Person 1", "Person 2", etc. in
  speaking order).
  - Yesterday: ...
  - Today: ...
  - Blockers: ... (or "none")

Then a single "Group decisions / followups" section listing anything not tied
to a single speaker.

Use plain ASCII. Do not invent updates that aren't in the transcript -- if a
speaker did not mention yesterday/today/blockers, write "(not stated)".

Session: {{session_title}}
Date: {{date}}

Transcript:
{{transcript}}
