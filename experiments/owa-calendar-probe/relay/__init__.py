"""OWA Calendar Probe relay (issue #69 Option C sandbox).

Intentionally a freestanding package; does NOT import from the
production meeting_notetaker tree. This keeps the experiment
isolated so changes here can never break a prod install.
"""
