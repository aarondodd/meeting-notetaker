__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
.env
.pytest_cache/
.coverage
.tox/
htmlcov/
dist/
build/
*.egg-info/
.mypy_cache/
.ruff_cache/

# IDE / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# faster-whisper model cache (downloaded on first run)
# Root-anchored so it does NOT match meeting_notetaker/models/ (the Python package).
/models/

# pyinstaller artifacts
*.toc
*.pkg
*.spec.bak

# Local-only ad-hoc fixtures
tests/data/private/
Output/

# OWA calendar probe (experiment/owa-calendar-probe; never built into prod).
# data/ holds raw capture JSON containing user calendar payloads; tests/fixtures
# carries the redacted snapshots that ARE tracked.
experiments/owa-calendar-probe/data/
!experiments/owa-calendar-probe/data/.gitkeep
